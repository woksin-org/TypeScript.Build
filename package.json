{
    "private": "true",
    "workspaces": [
        "Source/*"
    ],
    "scripts": {
        "build": "tsc -b Source/tsconfig.json",
        "clean": "yarn workspaces run clean",
        "lint": "yarn workspaces run lint",
        "lint:fix": "yarn workspaces run lint:fix",
        "ci": "yarn clean && yarn build && yarn lint"
    },
    "devDependencies": {
        "rimraf": "^3.0.0"
    }
}
