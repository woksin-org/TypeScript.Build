{
    "private": "true",
    "workspaces": [
        "Source/**"
    ],
    "scripts": {
        "build": "tsc -b Source/tsconfig.json",
        "change-version": "yarn wsrun version --no-git-tag-version --no-commit-hooks --new-version"
    },
    "devDependencies": {
        "@typescript-eslint/parser": "^3.6.0",
        "eslint": "^7.4.0",
        "rimraf": "^3.0.0",
        "wsrun": "^5.2.1"
    },
    "peerDependencies": {
        "typescript": "^3.9.6"
    }
}
